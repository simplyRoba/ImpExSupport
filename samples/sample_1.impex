#% language.set();
$catalog=TestCatalog
$catalogVersion=catalogVersion(Catalog.uid($catalog), CatalogVersion.version(Staged))

INSERT_UPDATE Product;$catalogVersion;code(uid)[unique=true];reference(code);attribute;list;
;;testRow1;13534;"eins";item,item,item;
;;testRow3;13534;"eins";item,item,item;

;;testRow4;13534;"eins";item,item,item;

# comment
remove Item;code(uid);attribute;attribute2;
;testremoveRohatruew;'zwei';false;

