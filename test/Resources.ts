
import * as path from "path";

const fixtures: string = path.normalize(path.join("..", "..", "fixtures"));

export const TestColumnHiglighterImpex: string = path.normalize(path.join(__dirname, fixtures, "TestColumnHighlighter.impex"));
export const Sample1Impex: string = path.normalize(path.join(__dirname, fixtures, "sample_1.impex"));
